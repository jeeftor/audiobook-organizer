name: Release
on:
  push:
    tags:
      - 'v*'
permissions:
  contents: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Debug conditions
        run: |
          echo "Event name: ${{ github.event_name }}"
          echo "Base ref: ${{ github.event.base_ref }}"
          echo "Ref: ${{ github.ref }}"
          git fetch origin master
          if git merge-base --is-ancestor HEAD origin/master; then
            echo "Tag is on master branch: true"
          else
            echo "Tag is on master branch: false"
          fi

  pre-commit:
    runs-on: ubuntu-latest
    needs: debug
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v4
        with:
          go-version: 'stable'
      - name: Install Go dependencies
        run: go mod tidy
      - name: Set up Python
        uses: actions/setup-python@v5
      - name: Install pre-commit
        run: pip install pre-commit
      - name: Run pre-commit
        run: pre-commit run --all-files

  # Build on each platform (CLI + GUI native compilation)
  build:
    needs: [pre-commit]
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
          - os: macos-14  # ARM64 runner
            goos: darwin
          - os: windows-latest
            goos: windows
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Verify on master
        if: matrix.os == 'ubuntu-latest'
        run: |
          git fetch origin master
          if ! git merge-base --is-ancestor HEAD origin/master; then
            echo "Tag is not on master branch"
            exit 1
          fi

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'
          cache: true
          cache-dependency-path: |
            go.sum
            audiobook-organizer-gui/go.sum

      - name: Set up Node.js (for GUI)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install npm dependencies (for GUI)
        working-directory: audiobook-organizer-gui/frontend
        run: npm install

      - name: Build with goreleaser (single platform)
        run: |
          go run github.com/goreleaser/goreleaser/v2@latest build --single-target --snapshot --clean
        env:
          CGO_ENABLED: 1

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.goos }}
          path: dist/
          retention-days: 1

  # Create release with all platform artifacts
  release:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Download all platform artifacts
      - name: Download Linux artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-linux
          path: dist/

      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-darwin
          path: dist/

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-windows
          path: dist/

      - name: List dist contents
        run: ls -R dist/

      # Generate a GitHub App token for both repositories
      - name: Generate GitHub App Token
        id: generate_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: >-
            audiobook-organizer,
            homebrew-tap

      # Configure Git for commit authoring
      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      # Install frontend dependencies (needed for goreleaser hooks)
      - name: Install frontend dependencies
        working-directory: audiobook-organizer-gui/frontend
        run: npm install

      # Run GoReleaser to create archives and release (skip build since we have pre-built binaries)
      - name: GoReleaser
        uses: goreleaser/goreleaser-action@v6
        timeout-minutes: 10
        with:
          distribution: goreleaser
          version: latest
          args: continue --merge
        env:
          GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}

      # Backup artifacts in case release upload fails
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: audiobook-organizer-binaries-failed
          path: dist/*
          retention-days: 5

      - name: Print Version Info
        run: |
          echo "GitHub Ref: ${{ github.ref }}"
          echo "GitHub Ref Name: ${{ github.ref_name }}"
          echo "Version to be released: ${GITHUB_REF#refs/tags/v}"
